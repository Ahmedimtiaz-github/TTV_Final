# Visual Templates

This directory contains PIL-based visual templates used by the fallback renderer in `visual_gen.py`.

The fallback renderer creates simple but readable placeholder frames when heavy visual models are not available, ensuring the pipeline can run end-to-end without external dependencies.
